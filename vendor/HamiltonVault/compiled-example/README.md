# Compiled example

The checked-in SQL uses repository-relative CSV paths. Execute it from the
repository root against a scratch DuckDB database. Generate fresh artifacts
with `hamilton-vault build` for your own project and run timestamp.

`run_results.json` records the verified demonstration load. Use the Hamilton
runner for managed loading, audit and source checks; see the main README.
